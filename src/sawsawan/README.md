# Integration Config

This directory contains app-level integration setup for ulam packages.

It wires together i18n (calamansi), UI components (ube), announcements (taho), and a11y debug tools (rogers) via the `@ulam/sawsawan` integration layer.

For framework documentation, see [@ulam/sawsawan](https://github.com/a11yfred/ulam).
